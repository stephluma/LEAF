I couldn’t complete your requested org-wide Launchpad discovery yet because this first org listing call only returned page 1 (100 repos) and there are more results.

From what I can verify so far:

In software org page 1, I do not see a repo name explicitly containing launchpad.
I have not yet confirmed any repo with:
report.php using the switch + file_exists("templates/reports/{$action}.tpl") resolver pattern, and
templates/reports/lp_*.tpl files (e.g., lp_impact.tpl, lp_help_library.tpl, etc.)
So at this moment, I cannot claim Launchpad was found, and I’m not assuming it matches the other apps.

If you want, I’ll continue immediately by scanning:

remaining org repo pages, and
code search across candidate repos for lp_impact, lp_help_library, launchpad, and templates/reports/lp_*.tpl.
For direct browsing, here’s the org repositories UI page:
https://va.ghe.com/orgs/software/repositories
