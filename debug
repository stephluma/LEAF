Great news: this is already implemented in LEAF_Request_Portal/report.php, and it uses exactly the iframe query param pattern you described.

Direct answer
report.php does branch on $_GET['iframe'] at render time:

software / LEAF / LEAF_Request_Portal / report.php
if (!isset($_GET['iframe']))
{
    $main->display(customTemplate('main.tpl'));
}
else
{
So if iframe is present (any value), it renders main_iframe.tpl instead of main.tpl.
That means the likely issue is in template structure (main_iframe.tpl still includes header/nav), or the wrong report.php endpoint/environment is being hit.

What I found for your 5 asks
Where header/nav gets included / decision point
In LEAF_Request_Portal/report.php, menu/header content is prepared unconditionally before display:
assigns login/menu/tab/title/city/revision
then chooses main.tpl vs main_iframe.tpl based on isset($_GET['iframe']).
Whether $_GET['iframe'] is referenced
Yes, clearly in:
LEAF_Request_Portal/report.php
also similar pattern in LEAF_Nexus/report.php
also in LEAF_Nexus/admin/index.php
So this is an existing convention, not new.
Existing alternate mechanism
I did not find evidence (in returned results) of a different canonical flag like embed, noheader, etc.
The established pattern appears to be switching to main_iframe.tpl when iframe is present.
Minimal safe approach (if behavior is wrong)
Keep existing convention (iframe presence) for compatibility.
Verify/fix main_iframe.tpl so it excludes header/nav while preserving required body/scripts.
If you want stricter semantics, you could change check to ($_GET['iframe'] ?? '') === '1', but that may break existing callers relying on presence-only behavior. Safer to keep presence check unless you control all callers.
Potential tricky parts
menu and login are assigned before template selection, so even iframe mode has those variables available.
If main_iframe.tpl accidentally renders them, you’ll still see nav/header.
Any JS/CSS expected from full layout might be coupled into main.tpl; ensure main_iframe.tpl still includes needed assets for report body interactivity.
Important limitation
Code search tool responses are capped and may be incomplete. To review full matches in GitHub UI, use:

https://va.ghe.com/software/LEAF/search?q=repo%3Asoftware%2FLEAF+iframe&type=code
https://va.ghe.com/software/LEAF/search?q=repo%3Asoftware%2FLEAF+main_iframe.tpl&type=code
https://va.ghe.com/software/LEAF/search?q=repo%3Asoftware%2FLEAF+customTemplate%28%27main.tpl%27%29&type=code
If you want, next step I can inspect main.tpl vs main_iframe.tpl (and any custom template overrides) and pinpoint exactly why header/nav is still showing with &iframe=1.
