convention, so an unprocessed/un-rendered page degrades to inert comments rather than broken markup. STATIC PLACEHOLDER — this page isn't Smarty-rendered yet. Both flags default to false below the {if} blocks; since the {if}/{/if} wrapper itself is an inert HTML comment when raw, the override line beneath still runs unconditionally, so both currently evaluate to true here — useful for visually testing the gated Internal-section items without a real server. Once this page is wired up to Smarty, this already renders correctly as-is — no changes needed beyond filling in the real group ID. REPLACE_ME_LEADERSHIP_GROUP_ID needs that real Smarty group ID. -->
