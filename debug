I found the resolver pattern, but not in Launchpad yet from the data returned so far.

What I can confirm right now
In this repo snapshot, both of the discovered report.php files (LEAF_Nexus/report.php and LEAF_Request_Portal/report.php) resolve ?a= like this:

read $_GET['a'] into $action (after XSS scrub)
switch ($action) for a few explicit special cases
otherwise fallback to: file_exists("templates/reports/{$action}.tpl")
if that template exists, it renders that page
So this is not a fixed lookup table of allowed strings in these files; it’s mostly a filename convention / dynamic template existence check.

That means an alias like ?a=impact alongside ?a=lp_impact is typically feasible by either:

adding an explicit switch case mapping impact -> lp_impact, or
adding a second template filename (impact.tpl) that reuses/includes the existing one (if your templating structure supports that cleanly).
What I still need to finish your Launchpad-specific ask
Your question is specifically about /launchpad/report.php and all recognized lp_* actions.
The initial code search didn’t return Launchpad files or lp_* action references yet, so I can’t truthfully list the Launchpad action set from this result alone.

If you want, I can continue immediately by scanning the repo for:

any launchpad/report.php (or equivalent front controller),
all templates/reports/lp_*.tpl,
any backend checks/redirects keyed on lp_* action names (permissions, logging, analytics, menu builders, JS URL constructors), and then give you the exact complete list and concrete “alias patch” location for Launchpad.
