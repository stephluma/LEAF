Direct finding
LEAF_Start_Request.tpl does not contain any header/nav-like block.
And spotlight_direct.tpl does not exist at LEAF_Request_Portal/templates/reports/spotlight_direct.tpl in this commit/path.

So the “duplicate header” is not coming from LEAF_Start_Request.tpl itself.

1) Full content: LEAF_Start_Request.tpl
Here is the complete file exactly as fetched:

software / LEAF / LEAF_Request_Portal / templates / reports / LEAF_Start_Request.tpl
<style>
#content {
    margin: 1rem;
}
p, .card {
    font-size: 1rem;
2) Full content: spotlight_direct.tpl
Could not be fetched because that file path does not exist in this repo/commit:

LEAF_Request_Portal/templates/reports/spotlight_direct.tpl → not found.
3) Recursive includes
LEAF_Start_Request.tpl has no {include ...} statements, so recursion ends there for that action template.

4) Exact header/nav-like block lines
In LEAF_Start_Request.tpl: none found (no header, menu, nav, logo, login/user bar markup).

5) Is isIframe assigned upstream for report templates?
From previously fetched LEAF_Request_Portal/report.php, isIframe is not assigned into $t_form before:

PHP
$t_form->fetch("reports/{$action}.tpl")
So if you want report body templates to react to iframe context, you need to assign it there.

6) Final proposed diff (review only, no edits)
A) report.php: pass iframe flag into report body templates
software / LEAF / LEAF_Request_Portal / report.php
v2
@@
             $t_form->assign('app_css_path', APP_CSS_PATH);
             $t_form->assign('app_js_path', APP_JS_PATH);
+            $t_form->assign('isIframe', isset($_GET['iframe']) ? 1 : 0);

             $powerQueryURL = "https://" . AUTH_URL . "/report";
B) Body template wrap
For LEAF_Start_Request.tpl, there is no offending header block to wrap.

For spotlight_direct, once we locate its actual rendered template file, wrap only that header-like block:

<actual
<!--{if !$isIframe}-->
    <!-- offending header/nav-like markup block -->
<!--{/if}-->

If you want, I can do one targeted follow-up to resolve where a=spotlight_direct is actually implemented (possibly outside templates/reports/spotlight_direct.tpl, e.g., custom template override, different case/path, or dynamic report resolver), then provide the exact block and exact line-anchored diff.s
