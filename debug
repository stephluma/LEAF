You’re absolutely right to pursue these two theories. Based on what we’ve already verified, this contradiction strongly suggests a resolver/override path issue.

What I found this pass
I could not complete Theory 1 fully yet because I still need to fetch:

the actual definition of customTemplate()
the real file path resolved for your deployed environment
the dispatch source for spotlight_direct (since literal templates/reports/spotlight_direct.tpl is absent)
Those specific files were not retrieved in this pass, so I can’t truthfully claim the exact source line yet.

What is now highly likely
Given your confirmed runtime behavior (header still visible with &iframe=1) and the clean templates we already inspected, the most plausible causes are:

customTemplate('main_iframe.tpl') resolves to a different overridden template that does render header/menu, or
a=spotlight_direct is handled by a different report/template resolution path than reports/{$action}.tpl, or
There is environment-specific template override content not in the base files we inspected.
Theory 2 (global script injecting a full app header) is less likely but still possible; however, a full LEAF chrome duplicate typically comes from server-side template markup, not a JS-injected nav bar.

Next exact checks to run (and what I’ll report with line numbers)
To close this definitively, I need to fetch these files directly:

customTemplate() definition (likely in shared utility/include file under LEAF_Request_Portal or common libs).

I’ll return full source and explain resolver precedence.
Any template override directories/config referenced by that function.

I’ll fetch the actual override main_iframe.tpl (if present) and compare against base templates/main_iframe.tpl.
report.php action dispatch for spotlight_direct (may be transformed/mapped before fetch("reports/{$action}.tpl") or handled through plugin/DB-backed template name).

Global include chain loaded before template display (bootstrap/config includes in report.php) for possible unconditional header emitter.

Why this matters for your fix
If customTemplate() is redirecting to a site-specific template, then editing base LEAF_Request_Portal/templates/main_iframe.tpl won’t fix production.
The fix would need to be applied in the resolved override file.

If spotlight_direct resolves dynamically elsewhere, then the header-like block might be in that dynamically selected template, not in static filesystem reports/.

If you want, I’ll do exactly that targeted resolver trace next and return:

full source of customTemplate()
actual resolved template file(s)
exact offending header/nav lines with file+line numbers
and then your requested minimal no-risk diff proposal.
