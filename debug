You’re on the right track, but I can’t yet prove the exact offending body block from the data returned so far.

What I could confirm
In LEAF_Request_Portal/report.php, body rendering is generic:

PHP
$main->assign('body', $t_form->fetch("reports/{$action}.tpl"));
So:

a=LEAF_Start_Request → LEAF_Request_Portal/templates/reports/LEAF_Start_Request.tpl
a=spotlight_direct → LEAF_Request_Portal/templates/reports/spotlight_direct.tpl
And main_iframe.tpl renders {$body} directly, so anything header-like must be inside those report templates (or nested includes they call).

Why I can’t show the exact leak block yet
The code-search responses are capped and were incomplete, and we haven’t retrieved:

templates/reports/LEAF_Start_Request.tpl
templates/reports/spotlight_direct.tpl
any nested includes they reference
So I can’t honestly show a definitive “this exact header block is the bug” yet.

Proposed minimal fix (once block is identified)
Use existing convention name: isIframe (already seen elsewhere in this repo templates).

In offending report template block:

Smarty
<!--{if !$isIframe}-->
  ...header/menu-like block...
<!--{/if}-->
And in report.php, pass flag into $t_form before fetch:

PHP
$t_form->assign('isIframe', isset($_GET['iframe']) ? 1 : 0);
That is minimal/safe:

default non-iframe keeps current behavior
iframe explicitly suppresses only wrapped block
If you want, I can do one more pass to fetch those two exact template files and their includes, then give you the precise offending markup and exact wrap location before any edits.
