The fix
Replace:
js!str.includes('<!--')
with:
js!str.includes('<' + '!--')
This preserves identical runtime behavior (still checks for the substring <!--) but breaks up the literal 4-character sequence in the raw source, so the HTML parser can't mistake it for a comment-open while tokenizing the <script> element.
