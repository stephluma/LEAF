Prompt:
I need two specific fixes applied to this file. Do not change anything else.
Fix 1 — Remove the #header/#footer CSS suppression rule.
Find this in the <style> block:
css#header, #footer { display: none !important; }
Replace it with this comment:
css/* NOTE: #header/#footer suppression intentionally omitted —
   suppressChrome() in leaf_nav.js handles this when hash-routed.
   Adding it here bleeds into the outer document via #lpSwapHost. */
Fix 2 — Add a guard to the window.LEAF_BREADCRUMB assignment.
Find the breadcrumb script block. It will look something like this (exact label/href values may differ — match whatever is currently in the file):
html<script>
  window.LEAF_BREADCRUMB = [
    { label: 'Resources', href: '...' },
    { label: 'Idea Portal' }
  ];
</script>
Wrap the assignment in a guard so it only fires when the page is loaded standalone, not when injected via the launchpad hash router. Keep whatever label and href values are already in the file — only add the guard and the comment:
html<!-- Breadcrumb guard — only render standalone, not when hash-routed.
     When hash-routed, leaf_nav.js injects its own breadcrumb into #lpSwapHost
     and leaf_breadcrumb.js is blocklisted from re-execution. -->
<script>
  if (!document.getElementById('lpSwapHost')) {
    window.LEAF_BREADCRUMB = [
      /* ...existing values unchanged... */
    ];
  }
</script>
That's all. No other changes.
