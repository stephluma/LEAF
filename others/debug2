console.log(
  "lpSwapHost hidden?",
  document.getElementById("lpSwapHost").hasAttribute("hidden"),
);
console.log(
  "lpSwapHost innerHTML length:",
  document.getElementById("lpSwapHost").innerHTML.length,
);
console.log(
  "lp-main hidden?",
  document.getElementById("lp-main")
    ? document.getElementById("lp-main").hasAttribute("hidden")
    : "NOT FOUND",
);
console.log(
  "lp-main id now:",
  document.getElementById("lp-main")
    ? document.getElementById("lp-main").id
    : "NOT FOUND — probably reassigned",
);
console.log("main-content el:", document.getElementById("main-content"));
VM216:1 lpSwapHost hidden? true
VM216:5 lpSwapHost innerHTML length: 0
VM216:9 lp-main hidden? NOT FOUND
VM216:15 lp-main id now: NOT FOUND — probably reassigned
VM216:21 main-content el: <main id=​"main-content">​…​</main>​
undefined
